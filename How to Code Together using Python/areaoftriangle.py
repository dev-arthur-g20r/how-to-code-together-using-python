import os
def areaOfTriangle(b,h):
	area = float(base) * float(height) / 2
	print ("Area of triangle is %f" % float(area))
	return
	
base = input("Base of triangle: ")
height = input("Height of triangle: ")
areaOfTriangle(float(base),float(height))
os.system("pause")